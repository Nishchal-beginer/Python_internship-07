# Python_internship-07

This project is a Python tool that automatically resizes all images in a folder using Pillow.
It reads images from an input directory, processes them in batch, and saves the resized versions to an output folder.
Use it to quickly automate bulk image resizing for projects, portfolios, or optimization tasks.
